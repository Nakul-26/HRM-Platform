# Copy to .env.local for local `next dev`.
# Must match apps/gateway/wrangler.jsonc's ROOT_DOMAIN — this app resolves
# tenants from its own subdomain the same way the Gateway does.
ROOT_DOMAIN=lvh.me
NEXT_PUBLIC_ROOT_DOMAIN=lvh.me
GATEWAY_PORT=8787
