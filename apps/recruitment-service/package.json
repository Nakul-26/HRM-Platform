{
  "name": "@hrm/recruitment-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "test:integration": "vitest run --config vitest.integration.config.ts"
  },
  "dependencies": {
    "@hrm/auth": "workspace:*",
    "@hrm/config": "workspace:*",
    "@hrm/db": "workspace:*",
    "@hrm/logger": "workspace:*",
    "@hrm/types": "workspace:*",
    "aws4fetch": "^1.0.20",
    "drizzle-orm": "^0.38.0",
    "hono": "^4.6.14",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241224.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.8",
    "wrangler": "^3.99.0"
  }
}
